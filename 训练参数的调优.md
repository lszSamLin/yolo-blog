# 训练参数的调优

## 引言

超参数调优是深度学习训练中最具挑战性的环节。Ultralytics YOLO 提供了丰富的超参数配置选项和强大的自动调优工具。本文系统性地介绍训练参数的调优策略、调优流程和常见问题诊断，并结合实际训练结果进行案例分析。

> **参考来源**：[Ultralytics Train Configuration](https://docs.ultralytics.com/modes/train/) | [Ultralytics Hyperparameter Tuning](https://docs.ultralytics.com/guides/hyperparameter-tuning/)


## 一、超参数完整分类与详解

### 1.1 数据相关参数

| 参数 | 默认值 | 推荐范围 | 说明 |
|------|--------|---------|------|
| `data` | - | - | 数据集配置文件路径（必需） |
| `imgsz` | 640 | [320, 1920] | 输入分辨率，必须是32的倍数 |
| `batch` | -1 | [1, 128] | 批次大小，-1=自动选择 |
| `epochs` | 100 | [10, 1000] | 训练轮次 |
| `patience` | 100 | [10, 200] | 早停耐心值 |
| `cache` | False | False/disk/True | 数据缓存策略 |
| `fraction` | 1.0 | [0.01, 1.0] | 数据集采样比例（快速实验） |
| `seed` | 0 | [0, 2³¹] | 随机种子（可重复性） |

### 1.2 优化器与学习率参数

| 参数 | 默认值 | 推荐范围 | 说明 |
|------|--------|---------|------|
| `optimizer` | "SGD" | SGD/Adam/AdamW | 优化器选择 |
| `lr0` | 0.01 | [0.0001, 0.1] | 初始学习率 |
| `lrf` | 0.01 | [0.001, 0.1] | 最终学习率比例 |
| `momentum` | 0.937 | [0.8, 0.99] | SGD动量/Adam beta1 |
| `weight_decay` | 0.0005 | [0, 0.05] | L2正则化 |
| `warmup_epochs` | 3.0 | [0, 10] | 预热轮数 |
| `warmup_momentum` | 0.8 | [0.7, 0.95] | 预热阶段动量 |
| `warmup_bias_lr` | 0.1 | [0.01, 0.5] | 预热阶段bias学习率 |
| `close_mosaic` | 10 | [0, 30] | 最后N轮关闭mosaic |

### 1.3 损失权重参数

| 参数 | 默认值 | 推荐范围 | 说明 |
|------|--------|---------|------|
| `box` | 7.5 | [1.0, 20.0] | bbox回归损失(CIoU)权重 |
| `cls` | 0.5 | [0.1, 2.0] | 分类损失(Varifocal)权重 |
| `dfl` | 1.5 | [0.5, 5.0] | 分布焦点损失(DFL)权重 |
| `label_smoothing` | 0.0 | [0.0, 0.5] | 标签平滑系数 |
| `nbs` | 64 | [16, 256] | 名义批次大小(学习率缩放) |

### 1.4 数据增强参数

| 参数 | 默认值 | 推荐范围 | 说明 |
|------|--------|---------|------|
| `hsv_h` | 0.015 | [0, 0.1] | HSV色调变化 |
| `hsv_s` | 0.7 | [0, 1.0] | HSV饱和度变化 |
| `hsv_v` | 0.4 | [0, 1.0] | HSV亮度变化 |
| `degrees` | 0.0 | [0, 45] | 旋转角度 |
| `translate` | 0.1 | [0, 0.5] | 平移比例 |
| `scale` | 0.5 | [0, 1.0] | 缩放比例 |
| `shear` | 0.0 | [0, 10] | 剪切角度 |
| `perspective` | 0.0 | [0, 0.001] | 透视变换 |
| `flipud` | 0.0 | [0, 1.0] | 上下翻转概率 |
| `fliplr` | 0.5 | [0, 1.0] | 左右翻转概率 |
| `mosaic` | 1.0 | [0, 1.0] | Mosaic概率 |
| `mixup` | 0.0 | [0, 0.5] | MixUp概率 |
| `copy_paste` | 0.0 | [0, 0.5] | Copy-Paste概率 |

### 1.5 其他参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `amp` | True | 自动混合精度训练 |
| `resume` | False | 从断点恢复训练 |
| `save` | True | 保存模型权重 |
| `save_period` | -1 | 每隔N轮保存(-1=仅保存最佳和最后) |
| `device` | 0 | GPU设备(0/1/cpu/[0,1]) |
| `workers` | 8 | 数据加载线程数 |
| `plots` | True | 生成训练曲线图 |


## 二、学习率调优深度解析

### 2.1 学习率调度详解

Ultralytics 使用 **Linear Warmup + Cosine Decay** 调度策略：

```
学习率变化曲线:
              lr0 (0.01)
  ╮
  ╲
  ╲
  ╲
  ╲
  ╲
  lr0×lrf ╲  (0.0001)
  → epochs
        0    3(warmup)    100(结束)

数学公式:
  Warmup阶段 (epoch < warmup_epochs):
    lr = lr0 × (epoch / warmup_epochs)

  Cosine Decay阶段 (epoch >= warmup_epochs):
    lr = lrf × lr0 + 0.5 × lr0 × (1 - lrf) × (1 + cos(π × (epoch - warmup) / (epochs - warmup)))

```

### 2.2 学习率调优策略

```python
#
# 场景1: 数据集较大（> 10000 张）
#
model.train(
    lr0=0.01,          # 标准初始学习率
    lrf=0.01,          # 最终学习率 = 0.0001
    warmup_epochs=3,
    epochs=100,
)

#
# 场景2: 数据集较小（< 1000 张）
#
model.train(
    lr0=0.001,         # 降低学习率，避免过拟合
    lrf=0.01,
    warmup_epochs=5,   # 延长预热
    epochs=200,        # 增加训练轮次
)

#
# 场景3: 迁移学习（微调预训练模型）
#
model.train(
    lr0=0.001,         # 较低的初始学习率
    lrf=0.1,           # 较高的最终学习率（衰减幅度小）
    warmup_epochs=2,
    epochs=50,         # 较少的轮次
)

#
# 场景4: 从头训练
#
model.train(
    lr0=0.01,          # 标准学习率
    lrf=0.01,
    warmup_epochs=5,   # 较长的预热
    epochs=300,        # 更多轮次
)

```

### 2.3 不同优化器的学习率选择

```python
# SGD（推荐，默认）
# 优点: 泛化性好，收敛稳定
# 缺点: 需要手动调参
optimizer="SGD", lr0=0.01, momentum=0.937, weight_decay=0.0005

# Adam
# 优点: 自适应学习率，收敛快
# 缺点: 泛化性可能略差
optimizer="Adam", lr0=0.001, weight_decay=0.0001

# AdamW
# 优点: Adam + 解耦的权重衰减
# 缺点: 可能需要更多调参
optimizer="AdamW", lr0=0.001, weight_decay=0.01

```

### 2.4 学习率调优实战案例

```python
#
# 实际案例：基于自身数据集的学习率调优
#

# 案例1: 小数据集（< 500 张）+ 迁移学习
# 背景: 自行拍摄的单类别数据集，目标明确
model = YOLO("yolov8n.pt")
model.train(
    data="data.yaml",
    epochs=150,
    imgsz=640,
    batch=16,
    lr0=0.002,          # 较低学习率（迁移学习场景）
    lrf=0.7,            # 高lrf（衰减幅度小，保留学习能力）
    warmup_epochs=3,
    close_mosaic=10,
    patience=50,
)
# 结果: 153轮后 mAP50-95 达到 93.93%，训练稳定无过拟合

# 案例2: 大数据集（> 5000 张）
# 背景: 多类别复杂场景
model = YOLO("yolov8s.pt")
model.train(
    data="data.yaml",
    epochs=100,
    lr0=0.01,           # 标准学习率
    lrf=0.01,           # 充分衰减
    batch=32,           # 大批次（数据量大）
)

# 案例3: 高精度要求（小目标检测）
# 背景: 目标占比小，需要高精度定位
model = YOLO("yolov8m.pt")
model.train(
    data="data.yaml",
    epochs=200,
    imgsz=1280,         # 大输入尺寸
    lr0=0.003,          # 中等学习率
    box=10.0,           # 增大定位损失权重
    dfl=2.0,            # 增大DFL权重
)

```


## 三、批次大小调优

### 3.1 批次大小的影响

```
大批次（batch=32+）:
  ✓ 训练更稳定，收敛曲线平滑
  ✓ 充分利用GPU并行计算
  ✓ 训练速度更快（每个epoch时间短）
  ✗ 显存占用大
  ✗ 可能泛化性略差（陷入尖锐极小值）
  ✗ 需要更大的学习率

小批次（batch=4-8）:
  ✓ 显存占用小
  ✓ 泛化性通常更好（噪声梯度帮助跳出尖锐极小值）
  ✓ 训练更稳定（对学习率更宽容）
  ✗ 训练速度较慢
  ✗ 需要更小的学习率
  ✗ BatchNorm统计可能不稳定

```

### 3.2 批次大小选择策略

```python
# 方法1: 自动选择（默认）
model.train(batch=-1)  # 根据显存自动选择最大批次

# 方法2: 手动指定
model.train(batch=16)  # 常见选择

# 方法3: 多GPU时
# Ultralytics 会自动计算等效批次大小
# 学习率会根据 batch/nbs 的比例进行缩放
model.train(batch=32, device="0,1,2,3")  # 4卡，等效批次 128

```

**学习率线性缩放规则（Linear Scaling Rule）**：

```
实际学习率 = lr0 × min(1, batch_size / nbs)
其中 nbs = 64（名义批次大小）

示例:
  batch=16, nbs=64 → 实际lr = 0.01 × (16/64) = 0.0025
  batch=64, nbs=64 → 实际lr = 0.01 × 1 = 0.01
  batch=128, nbs=64 → 实际lr = 0.01 × 1 = 0.01（ capped）

```

### 3.3 显存不足时的调整

```python
# 方法1: 减小批次
model.train(batch=4)

# 方法2: 减小输入尺寸
model.train(imgsz=320)

# 方法3: 关闭AMP
model.train(amp=False)

# 方法4: 使用梯度累积（需要自定义脚本）
# 累加多个小批次的梯度，然后更新一次参数

```

### 3.4 批次大小调优实战

```python
# 根据显存选择批次大小
def auto_select_batch(gpu_memory_gb, model_size="n"):
    """根据GPU显存自动推荐批次大小"""
    # 经验值：每个样本约需 0.5-1GB 显存（取决于模型大小和imgsz）
    memory_per_sample = {"n": 0.5, "s": 1.0, "m": 2.0, "l": 3.0, "x": 4.0}
    mem_per_sample = memory_per_sample.get(model_size, 1.0)
    batch = int(gpu_memory_gb / mem_per_sample)
    return max(1, min(batch, 128))  # 限制在 1-128 之间

# 使用示例
# gpu_memory_gb = 8  # 8GB显存
# recommended_batch = auto_select_batch(8, "n")  # 返回 16

```


## 四、输入尺寸调优

### 4.1 输入尺寸与精度/速度的关系

```
imgsz=320:
  - 速度: 最快（~3x @ 640）
  - 精度: 最低
  - 适用: 大目标检测、实时性要求高

imgsz=640:
  - 速度: 快
  - 精度: 高
  - 适用: 通用场景（默认推荐）

imgsz=1280:
  - 速度: 慢（~0.3x @ 640）
  - 精度: 最高
  - 适用: 小目标检测、精度优先

```

### 4.2 多尺度训练

```python
# YOLOv8 支持多尺度训练
# 每 10 轮随机选择输入尺寸
model.train(imgsz=640, close_mosaic=10)

# 手动指定多尺度范围
model.train(imgsz=[480, 560, 640, 720, 800])

```

### 4.3 根据目标尺度选择

```python
# 小目标检测（目标占比 < 32×32 像素）
model.train(imgsz=1280, scale=0.9)

# 大目标检测（目标占比 > 64×64 像素）
model.train(imgsz=320, scale=0.5)

# 混合尺度
model.train(imgsz=640)

```

### 4.4 输入尺寸调优实战

```python
# 案例：根据目标大小调整输入尺寸
# 小目标场景（目标在图像中占比 < 1%）
model = YOLO("yolov8m.pt")
model.train(
    data="data.yaml",
    imgsz=1280,         # 大输入尺寸，保留更多细节
    scale=0.9,          # 大范围缩放增强
    mosaic=1.0,         # 保持Mosaic增强
)

# 大目标场景（目标占比 > 10%）
model = YOLO("yolov8n.pt")
model.train(
    data="data.yaml",
    imgsz=320,          # 小输入尺寸，加速推理
    scale=0.3,          # 小范围缩放
)

```


## 五、损失权重调优

### 5.1 损失权重影响分析

```python
# 默认配置
box=7.5   # bbox回归损失权重（较高）
cls=0.5   # 分类损失权重（较低）
dfl=1.5   # DFL损失权重（中等）

# 为什么 box 权重最高？
# 因为 bbox 回归对最终 mAP50-95 影响最大
# CIoU Loss 本身数值较小，需要较高的权重

```

### 5.2 场景化调优

```python
# 场景1: 定位精度要求高（mAP50-95 低）
model.train(
    box=10.0,   # 增大 bbox 损失权重
    dfl=2.0,    # 增大 DFL 权重
    cls=0.5,
)

# 场景2: 分类精度要求高（某些类别混淆严重）
model.train(
    box=7.5,
    cls=1.0,    # 增大分类损失权重
    dfl=1.5,
)

# 场景3: 类别不平衡
model.train(
    cls=0.8,
    label_smoothing=0.1,  # 标签平滑
)

```

### 5.3 损失权重调优实战

```python
# 案例：根据 Loss 曲线调整权重
# 观察结果：
# - box_loss 已收敛但 mAP50-95 不高
# - cls_loss 仍然在下降
# → 说明定位精度是瓶颈，应增大 box 权重

model = YOLO("yolov8s.pt")
model.train(
    data="data.yaml",
    box=10.0,       # 从 7.5 增大到 10.0
    dfl=2.0,        # 从 1.5 增大到 2.0
    cls=0.5,        # 保持
    epochs=100,
)

```


## 六、自动超参数调优

### 6.1 内置 Tune 功能

```python
from ultralytics import YOLO

model = YOLO("yolov8s.pt")

# 使用 Ultralytics 默认的搜索空间
results = model.tune(
    data="data.yaml",
    epochs=100,
    plots=True,
    space="ultralytics",  # Ultralytics 默认搜索空间
    saves=True,           # 保存每次搜索的结果
)

# 搜索结果包含:
# - 最佳超参数组合
# - 每次搜索的 mAP
# - 超参数搜索空间

```

### 6.2 自定义搜索空间

```python
from ultralytics import YOLO

model = YOLO("yolov8s.pt")

# 自定义搜索空间
results = model.tune(
    data="data.yaml",
    epochs=50,
    searches=20,           # 搜索20次
    space="ultralytics",
    project="runs/tune",
    name="exp1",
)

```

### 6.3 使用 Optuna 进行超参数搜索

```python
import optuna
from ultralytics import YOLO

def objective(trial):
    # 定义搜索空间
    lr0 = trial.suggest_float("lr0", 0.001, 0.1, log=True)
    weight_decay = trial.suggest_float("weight_decay", 0.0001, 0.01, log=True)
    mosaic = trial.suggest_float("mosaic", 0.0, 1.0)
    mixup = trial.suggest_float("mixup", 0.0, 0.5)
    hsv_h = trial.suggest_float("hsv_h", 0.0, 0.05)
    hsv_s = trial.suggest_float("hsv_s", 0.3, 1.0)

    # 训练模型
    model = YOLO("yolov8s.pt")
    results = model.train(
        data="data.yaml",
        epochs=50,
        lr0=lr0,
        weight_decay=weight_decay,
        mosaic=mosaic,
        mixup=mixup,
        hsv_h=hsv_h,
        hsv_s=hsv_s,
        verbose=False,
    )

    # 返回验证指标（使用 mAP50-95 作为优化目标）
    return results.results_dict["metrics/mAP50-95(B)"]

# 创建优化器
study = optuna.create_study(direction="maximize")
study.optimize(objective, n_trials=20)

print("最佳参数:", study.best_params)
print("最佳 mAP50-95:", study.best_value)

```


## 七、标准调优流程

### 7.1 推荐调优流程

```
Step 1: 基线训练
  - 使用默认参数
  - 记录各项指标
  - 建立性能基准

Step 2: 数据质量检查
  - 检查标注质量
  - 确认类别平衡
  - 查看数据集分布

Step 3: 输入尺寸调整
  - 根据目标大小调整 imgsz
  - 观察 mAP 变化

Step 4: 增强参数调整
  - 根据场景调整 HSV/几何增强
  - 调整 Mosaic/MixUp 强度

Step 5: 损失权重调整
  - 根据 loss 曲线调整 box/cls/dfl
  - 关注各损失项的相对大小

Step 6: 学习率调整
  - 调整 lr0/lrf/warmup
  - 观察收敛速度和最终精度

Step 7: 自动化搜索（可选）
  - 使用 tune() 或 Optuna

```

### 7.2 快速调优脚本

```python
from ultralytics import YOLO
import numpy as np

best_map = 0
best_params = {}

# 网格搜索
for imgsz in [640, 800]:
    for batch in [8, 16]:
        for lr0 in [0.005, 0.01, 0.02]:
            model = YOLO("yolov8s.pt")
            results = model.train(
                data="data.yaml",
                epochs=50,
                imgsz=imgsz,
                batch=batch,
                lr0=lr0,
                verbose=False,
            )
            map50_95 = results.results_dict["metrics/mAP50-95(B)"]

            if map50_95 > best_map:
                best_map = map50_95
                best_params = {
                    "imgsz": imgsz,
                    "batch": batch,
                    "lr0": lr0
                }
                print(f"New best: mAP50-95={map50_95:.3f}, params={best_params}")

print(f"\nBest params: {best_params}")
print(f"Best mAP50-95: {best_map:.3f}")

```


## 八、常见问题诊断

| 问题 | 诊断方法 | 解决方案 |
|------|---------|---------|
| 训练损失不下降 | 检查学习率是否过高 | 降低 lr0 |
| 过拟合（train val gap 大） | 增加 weight_decay / 增强 | weight_decay=0.001, 增强增强 |
| mAP50 高 mAP50-95 低 | 定位精度差 | 增大 box/dfl 权重 / 增大 imgsz |
| 某个类别 recall 低 | 检查该类别样本数量 | 增加该类别样本 / 增大 cls 权重 |
| 训练时间过长 | batch 太小 / imgsz 太大 | 增大 batch / 减小 imgsz |
| 显存不足 | batch 太大 / imgsz 太大 | 减小 batch / imgsz / 使用 AMP |
| 收敛震荡 | 学习率过高 | 降低 lr0 / 增大 warmup_epochs |
| 收敛过慢 | 学习率过低 | 增大 lr0 / 减小 lrf |


## 总结

超参数调优是一个迭代过程，核心原则：

1. **先建立基线**：使用默认参数跑一次完整训练
2. **一次只调一个参数**：便于定位参数影响
3. **关注损失曲线**：训练日志是最直接的诊断工具
4. **利用自动化工具**：`model.tune()` 和 Optuna 可以大幅提效
5. **记录实验结果**：使用 TensorBoard 或 W&B 跟踪每次实验

**结合实际训练的体会**：

从本次自行拍摄的单类别数据集训练经验来看，YOLOv8n 在默认参数下（lr0≈0.002, batch=16, imgsz=640）经过 153 轮训练即达到 mAP50-95=93.93%。这说明对于数据质量高、目标明确的场景，默认参数已经表现优异。调优的重点应放在：

- **小数据集**：降低 lr0、延长 warmup、增加 epochs
- **定位精度不足**：增大 imgsz、增大 box/dfl 权重
- **泛化性差**：增大 weight_decay、增强数据增强强度
- **训练不稳定**：减小 batch、增大 warmup_epochs


*参考资料：*
- *[Ultralytics Train Configuration](https://docs.ultralytics.com/modes/train/)*
- *[Ultralytics Hyperparameter Tuning](https://docs.ultralytics.com/guides/hyperparameter-tuning/)*


> **📌 系列导航**：[← 上一篇：基础yolo模型训练与指标分析](基础YOLO模型训练与指标分析.md) · [📖 导读目录](README.md) · [下一篇：训练管道中注意力注入与常见增强措施分析 →](训练管道中注意力注入与常见增强措施分析.md)
