# YOLO 模型量化深度解析：从原理到实践

## 引言

模型量化是将高精度浮点模型转换为低精度表示的技术，是深度学习模型部署优化的核心手段之一。在边缘设备（如 Rockchip NPU、GPU、Mobile）上部署 YOLO 模型时，量化可以显著减少模型体积、提升推理速度、降低功耗。本文深入解析量化的数学原理、实现方法和实际应用，并结合实际部署经验进行分析。

> **参考来源**：[TensorFlow Lite 量化指南](https://www.tensorflow.org/lite/performance/model_optimization) | [PyTorch Quantization](https://pytorch.org/docs/stable/quantization.html) | [Rockchip RKNN Quantization](https://github.com/airockchip/rknn-toolkit2)

## 一、量化的基本原理

### 1.1 什么是量化？

量化的本质
量化 = 将连续的值映射到离散的值
浮点表示:     weight = 0.123456789012345
量化表示:     weight_q = round(weight * scale) + zero_point
反量化:       weight ≈ weight_q * scale + zero_point
精度损失:     量化精度 = scale = (max - min) / (2^bits - 1)

### 1.2 量化的数学表示

**INT8 量化公式**：

```
正向量化:
    Q = round(P / S) + Z

反向反量化:
    P ≈ S × (Q - Z)

其中:
    P  = 浮点值 (FP32/FP16)
    Q  = 量化值 (INT8: [-128, 127])
    S  = 缩放因子 (scale)
    Z  = 零点 (zero_point)

典型参数:
    S  = (max(P) - min(P)) / 255
    Z  = round(-min(P) / S)  (clip to [0, 255])

```

**FP16 量化公式**：

```
FP16 格式 (IEEE 754-2008):
┌─────────────────────────────────────┐
│ Sign | Exponent (5 bits) | Mantissa (10 bits) │
│  1   |      5           |        10          │
└─────────────────────────────────────┘

值 = (-1)^sign × 2^(exponent-15) × (1 + mantissa/1024)

范围: ±65504
精度: ~3.3 位有效十进制数字

```

### 1.3 量化类型对比

```
量化类型          位宽    精度损失    速度提升    模型压缩比    典型应用场景
──────────────────────────────────────────────────────────────────────
FP32            32 bit   基准       基准        1x          训练
FP16            16 bit   ~0.1%      ~1.5x       2x          通用推理
INT8            8 bit    ~0.5-1%    ~2-3x       4x          边缘部署
INT4            4 bit    ~1-3%      ~4-6x       8x          极端压缩
NF4             4 bit    ~0.5-2%    ~4-6x       8x          LLM 场景

```

## 二、动态量化 vs 静态量化

### 2.1 动态量化（Dynamic Quantization）

动态量化流程

特点:
- 只在推理时进行量化
- 无需校准数据
- 仅对权重进行量化
- 激活值动态量化

流程:

FP32模型 → 量化权重 → 动态推理

▼  ▼
权重 INT8  激活 FP32
(预计算)  (运行时计算)

优点: 实现简单，无需校准数据
缺点: 精度损失较大，速度提升有限

```python
import torch
import torch.quantization as quant

# 动态量化示例
model = YOLO("yolov8s.pt").model
model.eval()

# 配置动态量化
model.qconfig = quant.get_default_qconfig("fbgemm")  # 或 "x86"

# 准备动态量化
quantized_model = quant.quantize_dynamic(
    model,
    {torch.nn.Linear, torch.nn.Conv2d},  # 要量化的层类型
    dtype=torch.qint8                      # 量化类型
)

# 保存量化模型
torch.save(quantized_model.state_dict(), "yolov8s_dynq.pt")

```

### 2.2 静态量化（Static Quantization）

静态量化流程

特点:
- 需要校准数据
- 预计算 scale 和 zero_point
- 权重和激活都量化
- 推理速度最快

流程:

FP32模型 → 量化权重 → 校准激活

▼  ▼
权重 INT8  激活 INT8 (预计算)
(预计算)

优点: 精度保持好，速度提升大
缺点: 需要校准数据，流程复杂

```python
import torch
import torch.quantization as quant
import numpy as np

# 准备校准数据
def calibration_data_loader(data_dir, batch_size=16):
    """校准数据加载器"""
    images = []
    for f in os.listdir(data_dir)[:100]:  # 使用100张校准图片
        img = cv2.imread(os.path.join(data_dir, f))
        img = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)
        img = cv2.resize(img, (640, 640))
        img = np.expand_dims(img.astype(np.float32), 0) / 255.0
        images.append(img)
    return torch.cat(images, 0)

# 静态量化
model = YOLO("yolov8s.pt").model
model.eval()

# 配置静态量化
model.qconfig = quant.get_default_qconfig("fbgemm")

# 插入量化/反量化节点
quantized_model = quant.prepare(model, inplace=True)

# 校准
calibration_data = calibration_data_loader("calibration_images/")
with torch.no_grad():
    quantized_model(calibration_data)

# 转换为量化模型
quantized_model = quant.convert(quantized_model, inplace=True)

# 保存
torch.save(quantized_model.state_dict(), "yolov8s_staticq.pt")

```

### 2.3 后训练量化（PTQ）vs 量化感知训练（QAT）

PTQ vs QAT 对比

PTQ (Post-Training Quantization)

流程: 训练(FP32) → 量化(PTQ) → 部署
优点: 简单快速，无需重新训练
缺点: 精度损失较大，尤其是对低精度(INT4)

QAT (Quantization-Aware Training)

流程: 训练(FP32) → 量化感知训练(QAT) → 部署
优点: 精度损失小，可恢复大部分精度
缺点: 需要额外训练时间，实现复杂

精度对比:

方法  FP32  PTQ  QAT

YOLOv8s INT8  44.9%  43.5%  44.2%
YOLOv8s INT4  44.9%  38.2%  43.8%

## 三、YOLO 模型量化实现

### 3.1 PyTorch 原生量化

```python
import torch
import torch.quantization as quant
from ultralytics import YOLO

def ptq_quantization(model_path="yolov8s.pt", output_path="yolov8s_int8.pt"):
    """
    后训练量化 (PTQ) - INT8
    """
    # 加载模型
    model = YOLO(model_path).model
    model.eval()
    
    # 配置量化
    model.qconfig = quant.get_default_qat_qconfig("fbgemm")
    
    # 准备 QAT 模型
    quant_model = quant.prepare_qat(model, inplace=True)
    
    # 如果没有校准数据，使用动态量化
    quant_model = quant.quantize_dynamic(
        model,
        {torch.nn.Conv2d},
        dtype=torch.qint8
    )
    
    # 保存
    torch.save(quant_model.state_dict(), output_path)
    print(f"量化模型已保存: {output_path}")
    
    return quant_model

def qat_quantization(model_path="yolov8s.pt", data_path="data.yaml", 
                     epochs=10, output_path="yolov8s_qat.pt"):
    """
    量化感知训练 (QAT)
    """
    # 加载模型
    model = YOLO(model_path)
    
    # 启动 QAT 训练
    model.train(
        data=data_path,
        epochs=epochs,
        quantize=True,  # 启用量化感知训练
        imgsz=640,
        batch=16,
    )
    
    # 保存量化模型
    model.save(output_path)
    print(f"QAT 模型已保存: {output_path}")
    
    return model

```

### 3.2 ONNX Runtime 量化

```python
import onnx
from onnxruntime.quantization import quantize_dynamic, quantize_static
from onnxruntime.quantization.shape_inference import quant_pre_process

def onnx_ptq_quantize(input_path, output_path, per_channel=True):
    """
    ONNX 后训练动态量化
    """
    # 加载 ONNX 模型
    model = onnx.load(input_path)
    
    # 动态量化 (仅权重)
    quantized_model = quantize_dynamic(
        model,
        output_path=output_path,
        weight_type=onnx.TensorProto.INT8,
        per_channel=per_channel,
    )
    
    print(f"动态量化完成: {output_path}")
    return quantized_model

def onnx_static_quantize(input_path, output_path, calibration_data_dir):
    """
    ONNX 静态量化 (需要校准数据)
    """
    # 加载 ONNX 模型
    model = onnx.load(input_path)
    
    # 静态量化
    quantized_model = quantize_static(
        model,
        input_path,
        output_path,
        calibration_data_reader=get_calibration_data(calibration_data_dir),
        per_channel=per_channel,
        op_types_to_quantize=["Conv"],
    )
    
    print(f"静态量化完成: {output_path}")
    return quantized_model

def get_calibration_data(data_dir):
    """校准数据生成器"""
    import os
    import cv2
    import numpy as np
    
    images = []
    for f in os.listdir(data_dir)[:100]:
        img = cv2.imread(os.path.join(data_dir, f))
        img = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)
        img = cv2.resize(img, (640, 640))
        img = np.expand_dims(img.astype(np.float32), 0) / 255.0
        images.append(img)
    
    def generator():
        for img in images:
            yield [img]
    
    return generator

```

### 3.3 TensorRT 量化

```python
import tensorrt as trt
from ultralytics import YOLO

def trt_quantization(model_path="best.pt", output_path="best_trt.int8.engine"):
    """
    TensorRT INT8 量化
    """
    logger = trt.Logger(trt.Logger.WARNING)
    
    # 加载模型
    builder = trt.Builder(logger)
    network = builder.create_network(
        1 << int(trt.NetworkDefinitionCreationFlag.EXPLICIT_BATCH)
    )
    
    parser = trt.OnnxParser(network, logger)
    
    # 解析 ONNX 模型
    onnx_path = model_path.replace(".pt", ".onnx")
    with open(onnx_path, "rb") as f:
        if not parser.parse(f.read()):
            print("解析失败:")
            for error in range(parser.num_errors):
                print(parser.get_error(error))
            return None
    
    # 配置 INT8 量化
    config = builder.create_builder_config()
    config.set_flag(trt.BuilderFlag.INT8)
    config.set_flag(trt.BuilderFlag.FP16)
    
    # 校准数据
    calibration_reader = TRTCalibrationReader("calibration_images/")
    config.int8_calibrator = calibration_reader
    
    # 设置最大内存
    config.set_memory_pool_limit(trt.MemoryPoolType.DEVICE, 1 << 20 * 512)
    
    # 构建引擎
    engine = builder.build_serialized_network(network, config)
    if engine is None:
        print("构建失败")
        return None
    
    # 保存引擎
    with open(output_path, "wb") as f:
        f.write(engine)
    
    print(f"TensorRT INT8 引擎已保存: {output_path}")
    return engine

class TRTCalibrationReader:
    """TensorRT 校准数据读取器"""
    
    def __init__(self, image_dir, calib_entries=100):
        self.image_dir = image_dir
        self.calib_entries = calib_entries
        self.images = self._load_images()
    
    def _load_images(self):
        import os
        images = []
        for f in sorted(os.listdir(self.image_dir))[:self.calib_entries]:
            img = cv2.imread(os.path.join(self.image_dir, f))
            img = cv2.resize(img, (640, 640))
            img = img.astype(np.float32).transpose(2, 0, 1) / 255.0
            images.append(img)
        return np.concatenate(images, axis=0)
    
    def get_calibration_data(self, tensor_name):
        return self.images
    
    def __len__(self):
        return self.calib_entries

```

### 3.4 RKNN 量化（瑞芯微 NPU）

```python
from rknn.api import RKNN
import cv2
import numpy as np
import glob

def rknn_quantization(onnx_path, rknn_path, target="rk3588", 
                      do_quantization=False, dataset_path=None):
    """
    RKNN 模型量化转换
    
    参数:
        onnx_path: ONNX 模型路径
        rknn_path: 输出的 RKNN 模型路径
        target: 目标平台 (rk3588/rk3568/rk3576)
        do_quantization: 是否启用量化
        dataset_path: 校准数据集路径 (INT8 量化需要)
    """
    # 创建 RKNN 实例
    rknn = RKNN()
    
    # 配置模型（注意：Toolkit2 中 config 必须在加载模型之前调用）
    print("--> Configuring model")
    ret = rknn.config(
        mean_values=[[0, 0, 0]],       # 归一化均值（与预处理一致）
        std_values=[[255, 255, 255]],  # 归一化方差
        target_platform=target,        # 目标芯片平台
    )
    if ret != 0:
        print(f"配置失败: {ret}")
        return None
    print("Done")
    
    # 加载 ONNX 模型
    print("--> Loading ONNX model")
    ret = rknn.load_onnx(model=onnx_path)
    if ret != 0:
        print(f"加载 ONNX 失败: {ret}")
        return None
    print("Done")
    
    # 构建模型 (根据 do_quantization 选择量化方式)
    print("--> Building model")
    if do_quantization:
        # INT8 量化：生成校准数据集文件（txt，每行一张图片路径，建议约 100 张代表性图片）
        images = sorted(glob.glob(f"{dataset_path}/*.jpg"))
        assert images, f"校准目录中未找到图片: {dataset_path}"
        with open("calibration_dataset.txt", "w") as f:
            f.write("\n".join(images[:100]))

        ret = rknn.build(
            do_quantization=True,
            dataset="calibration_dataset.txt",
            quantized_algorithm="normal",   # normal / mmse / kl_divergence
        )
        print("INT8 量化构建完成")
    else:
        # FP16 混合精度（不量化）
        ret = rknn.build(do_quantization=False)
        print("FP16 构建完成")
    
    if ret != 0:
        print(f"构建失败: {ret}")
        return None
    print("Done")
    
    # 导出 RKNN 模型
    print("--> Exporting RKNN model")
    ret = rknn.export_rknn(rknn_path)
    if ret != 0:
        print(f"导出失败: {ret}")
        return None
    print(f"RKNN 模型已保存: {rknn_path}")
    print("Done")
    
    # 释放资源
    rknn.release()
    
    return rknn_path

# 使用示例
# FP16 量化
rknn_quantization("best.onnx", "best_fp16.rknn", target="rk3588", do_quantization=False)

# INT8 量化
rknn_quantization(
    "best.onnx", 
    "best_int8.rknn", 
    target="rk3588",
    do_quantization=True,
    dataset_path="./calibration_images"
)

```

### 3.5 OpenVINO 量化

```python
from openvino.tools.mo import convert_model
from openvino.runtime import serialize
import openvino.runtime as ov

def openvino_quantization(onnx_path, output_path, precision="INT8"):
    """
    OpenVINO 模型量化
    """
    # 转换 ONNX 到 OpenVINO IR
    ov_model = convert_model(onnx_path)
    
    # 保存 FP32 模型
    serialize(ov_model, output_path.replace(".xml", "_fp32.xml"))
    
    if precision == "INT8":
        # INT8 量化 (需要校准数据)
        from openvino.tools.mo import compress_to_int8
        compress_to_int8(ov_model)
        
        # 保存量化模型
        serialize(ov_model, output_path.replace(".xml", "_int8.xml"))
    
    print(f"OpenVINO 量化完成: {output_path}")
    return ov_model

```

## 四、量化效果分析与评估

### 4.1 精度损失评估

```python
import numpy as np
from ultralytics import YOLO

def evaluate_quantization(model_path, quantized_path, data_path, tasks=["detect", "segment", "pose"]):
    """
    评估量化对模型精度的影响
    """
    results = {}
    
    for task in tasks:
        print(f"\n=== {task.upper()} 任务量化评估 ===")
        
        # 加载原始模型
        model_fp32 = YOLO(model_path)
        results[task] = {"fp32": {}, "quantized": {}}
        
        # 评估 FP32 模型
        val_fp32 = model_fp32.val(data=data_path, batch=16, verbose=False)
        results[task]["fp32"] = {
            "mAP50": val_fp32.box.map50,
            "mAP50-95": val_fp32.box.map,
            "precision": val_fp32.box.mp,
            "recall": val_fp32.box.mr,
        }
        print(f"FP32: mAP50={results[task]['fp32']['mAP50']:.3f}, "
              f"mAP50-95={results[task]['fp32']['mAP50-95']:.3f}")
        
        # 评估量化模型
        model_quant = YOLO(quantized_path)
        val_quant = model_quant.val(data=data_path, batch=16, verbose=False)
        results[task]["quantized"] = {
            "mAP50": val_quant.box.map50,
            "mAP50-95": val_quant.box.map,
            "precision": val_quant.box.mp,
            "recall": val_quant.box.mr,
        }
        print(f"Quant: mAP50={results[task]['quantized']['mAP50']:.3f}, "
              f"mAP50-95={results[task]['quantized']['mAP50-95']:.3f}")
        
        # 计算精度损失
        loss = results[task]["fp32"]["mAP50-95"] - results[task]["quantized"]["mAP50-95"]
        results[task]["accuracy_loss"] = loss
        print(f"精度损失: {loss:.3f}")
    
    return results

# 使用示例
results = evaluate_quantization(
    model_path="best.pt",
    quantized_path="best_int8.pt",
    data_path="data.yaml",
    tasks=["detect", "segment", "pose"]
)

```

### 4.2 性能对比

```python
import time
import numpy as np
from ultralytics import YOLO

def benchmark_quantization(model_path, quantized_path, imgsz=640, n_runs=100):
    """
    基准测试量化模型的推理速度
    """
    results = {}
    
    # FP32 基准
    model_fp32 = YOLO(model_path)
    
    # 预热
    dummy_input = np.random.randn(1, 3, imgsz, imgsz).astype(np.float32)
    for _ in range(10):
        model_fp32(dummy_input, verbose=False)
    
    # 计时
    times_fp32 = []
    for _ in range(n_runs):
        start = time.perf_counter()
        model_fp32(dummy_input, verbose=False)
        times_fp32.append(time.perf_counter() - start)
    
    results["fp32"] = {
        "avg_ms": np.mean(times_fp32) * 1000,
        "min_ms": np.min(times_fp32) * 1000,
        "max_ms": np.max(times_fp32) * 1000,
        "fps": 1.0 / np.mean(times_fp32),
    }
    print(f"FP32: avg={results['fp32']['avg_ms']:.1f}ms, "
          f"min={results['fp32']['min_ms']:.1f}ms, "
          f"fps={results['fp32']['fps']:.1f}")
    
    # 量化模型基准
    model_quant = YOLO(quantized_path)
    
    # 预热
    for _ in range(10):
        model_quant(dummy_input, verbose=False)
    
    # 计时
    times_quant = []
    for _ in range(n_runs):
        start = time.perf_counter()
        model_quant(dummy_input, verbose=False)
        times_quant.append(time.perf_counter() - start)
    
    results["quantized"] = {
        "avg_ms": np.mean(times_quant) * 1000,
        "min_ms": np.min(times_quant) * 1000,
        "max_ms": np.max(times_quant) * 1000,
        "fps": 1.0 / np.mean(times_quant),
    }
    print(f"Quant: avg={results['quantized']['avg_ms']:.1f}ms, "
          f"min={results['quantized']['min_ms']:.1f}ms, "
          f"fps={results['quantized']['fps']:.1f}")
    
    # 计算加速比
    speedup = results["fp32"]["avg_ms"] / results["quantized"]["avg_ms"]
    results["speedup"] = speedup
    print(f"加速比: {speedup:.2f}x")
    
    return results

```

### 4.3 量化效果总结

```
量化类型       模型大小      推理速度      mAP 损失     适用场景
─────────────────────────────────────────────────────────────────
FP32 (基准)    100%         1x          0%           训练/高精度需求
FP16           ~50%         ~1.5x       ~0.1%        通用推理
INT8 PTQ       ~25%         ~2.5x       ~0.5-1%      边缘设备
INT8 QAT       ~25%         ~2.5x       ~0.1-0.3%    高精度边缘部署
INT4           ~12%         ~4-6x       ~1-3%        极端压缩场景

```

## 五、量化最佳实践

### 5.1 选择合适的量化策略

```python
# 决策流程
def choose_quantization_strategy(task, accuracy_requirement, hardware):
    """
    根据任务和硬件选择合适的量化策略
    """
    strategies = {
        "训练阶段": "FP32",
        "高精度推理": "FP16 或 INT8 QAT",
        "边缘设备": "INT8 PTQ",
        "移动端": "INT8 或 INT4",
        "NPU部署": "RKNN INT8/FP16",
    }
    
    if accuracy_requirement == "high":
        return "INT8 QAT" if hardware == "GPU" else "FP16"
    elif accuracy_requirement == "medium":
        return "INT8 PTQ"
    else:
        return "INT4" if hardware == "mobile" else "INT8 PTQ"

```

### 5.2 校准数据准备

```python
import cv2
import numpy as np
import os

def prepare_calibration_dataset(image_dir, output_dir, n_samples=100, imgsz=640):
    """
    准备量化校准数据集
    
    要求:
    1. 数据应覆盖所有类别
    2. 包含各种光照条件
    3. 包含各种角度和尺度
    4. 数量建议: 100-500 张
    """
    images = []
    
    # 收集图像
    for f in sorted(os.listdir(image_dir)):
        if f.endswith(('.jpg', '.png', '.jpeg')):
            img = cv2.imread(os.path.join(image_dir, f))
            if img is not None:
                images.append(img)
                if len(images) >= n_samples:
                    break
    
    # 预处理并保存
    os.makedirs(output_dir, exist_ok=True)
    processed = []
    
    for i, img in enumerate(images):
        # 预处理（⚠️ 必须与部署预处理完全一致）
        # 部署用 letterbox 灰 114 时，校准图也必须预 letterbox，不能直接拉伸：
        # 拉伸校准会把 INT8 置信度磨平、mAP 显著劣化（RK3588 实测，见 README §八 实验）
        img = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)
        h, w = img.shape[:2]
        scale = min(imgsz / w, imgsz / h)
        nw, nh = int(w * scale), int(h * scale)
        img = cv2.resize(img, (nw, nh))
        canvas = np.full((imgsz, imgsz, 3), 114, dtype=np.uint8)   # letterbox 灰 114
        top, left = (imgsz - nh) // 2, (imgsz - nw) // 2
        canvas[top:top + nh, left:left + nw] = img
        img = canvas.astype(np.float32) / 255.0
        
        # 保存为 numpy 数组
        np.save(os.path.join(output_dir, f"calib_{i:04d}.npy"), img)
        processed.append(img)
        
        if (i + 1) % 10 == 0:
            print(f"已处理 {i + 1}/{len(images)} 张")
    
    print(f"校准数据集已保存到: {output_dir}")
    print(f"共 {len(processed)} 张校准图片")
    
    return processed

def generate_calibration_dataset_rknn(image_dir, output_file, n_samples=100):
    """
    生成 RKNN 校准数据集
    """
    images = prepare_calibration_dataset(image_dir, None, n_samples)
    
    # RKNN 校准数据格式: 生成器
    def dataset():
        for img in images:
            yield [img]
    
    return dataset

```

### 5.3 量化后的模型优化

```python
from ultralytics import YOLO

def optimize_quantized_model(model_path, quantized_path, optimizations=["clip", "fuse"]):
    """
    量化后模型优化
    """
    model = YOLO(model_path)
    
    # 导出量化模型
    model.export(
        format="onnx",
        simplify=True,           # ONNX 简化
        dynamic=False,           # 固定尺寸
    )
    
    # 后处理优化
    if "clip" in optimizations:
        # Clip 优化: 裁剪激活值范围
        pass
    
    if "fuse" in optimizations:
        # 算子融合: Conv+BN+Activations
        pass
    
    # 量化
    quantize_dynamic(
        model,
        output_path=quantized_path,
        weight_type=8,
    )
    
    return quantized_path

```

## 六、常见问题与解决

### 6.1 量化精度下降过多

```
问题: INT8 量化后 mAP 下降超过 2%

原因分析:
1. 校准数据不足或不具有代表性
2. 模型中有对量化敏感的算子 (如 Softmax, LayerNorm)
3. 激活值分布不均匀 (存在异常值)

解决方案:
1. 增加校准数据量 (100 → 500 张)
2. 使用 QAT 替代 PTQ
3. 对敏感算子保持 FP16
4. 使用混合精度量化

```

### 6.2 量化后推理结果不一致

```
问题: 量化后推理结果与 FP32 不一致

原因:
1. 量化引入了数值误差累积
2. 某些算子的量化实现有差异

解决方案:
1. 使用更高分辨率 (imgsz=1280)
2. 调整置信度阈值
3. 使用 QAT 重新训练
4. 检查量化配置参数

```

### 6.3 NPU 量化特定问题

```python
# RKNN 量化常见问题

# 问题1: INT8 量化后精度严重下降
# 解决: 增加校准数据，检查数据分布
rknn.config(
    mean_values=[[0, 0, 0]],
    std_values=[[255, 255, 255]],
    target_platform='rk3588',
)

# 问题2: 部分算子不支持 INT8
# 解决: 将这些算子保持 FP16
# 检查不支持的算子列表
# 升级 RKNN-Toolkit2 到最新版本

# 问题3: 校准数据加载失败
# 解决: 确保数据格式正确 (numpy array, float32)
def dataset():
    for img_path in calibration_images:
        img = cv2.imread(img_path)
        img = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)
        img = cv2.resize(img, (640, 640))
        img = np.expand_dims(img.astype(np.float32), axis=0) / 255.0
        yield [img]

```

## 总结

模型量化的核心要点：

1. **量化类型选择**：FP16 精度损失最小，INT8 性价比最高，INT4 压缩比最大
2. **PTQ vs QAT**：PTQ 简单快速，QAT 精度高但需要额外训练
3. **校准数据**：INT8 量化需要具有代表性的校准数据集（100-500张）
4. **精度-速度权衡**：根据应用场景选择合适的量化策略
5. **工具链选择**：根据目标硬件选择相应的量化工具（TensorRT/OpenVINO/RKNN）

**结合实际部署的经验**：

从实际部署经验来看，YOLO 模型在 RK3588 上的量化建议如下：
- **FP16 量化**：适合对精度要求较高的场景，模型体积减半，推理速度提升约 1.5 倍，精度损失几乎可忽略（< 0.1%）
- **INT8 量化**：适合对速度要求较高的场景，模型体积变为原来的 1/4，推理速度提升约 2-3 倍，精度损失通常在 0.5-1% 以内
- **校准数据准备**：建议使用与实际部署场景一致的图像作为校准数据，覆盖各种光照条件和目标尺度
- **精度验证**：量化后务必在测试集上重新评估 mAP50-95，确保精度损失在可接受范围内

*参考资料：*
- *[PyTorch Quantization Documentation](https://pytorch.org/docs/stable/quantization.html)*
- *[TensorRT Quantization](https://docs.nvidia.com/deeplearning/tensorrt/developer-guide/index.html#quantization)*
- *[RKNN-Toolkit2 Quantization](https://github.com/airockchip/rknn-toolkit2/blob/master/docs/en/02_rknn_user_guide/01_toolkit_user_guide.md)*
- *[OpenVINO Model Optimizer](https://docs.openvino.ai/latest/openvino_docs_MO_DG_prepare_model_convert_model_Convert_Model_From_ONNX.html)*

> **📌 系列导航**：[← 上一篇：yolo模型评估体系与性能分析](YOLO模型评估体系与性能分析.md) · [📖 导读目录](README.md) · [下一篇：Yolo模型的转换与rknn-toolkit相关工具链的使用 →](YOLO模型的转换与rknn-toolkit相关工具链的使用.md)
